{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/memory-mcp"],
      "description": "Persistent knowledge graph for stakeholder and project context"
    },
    "atlassian": {
      "type": "remote",
      "url": "https://mcp.atlassian.com/v1/sse",
      "description": "Jira and Confluence integration - requires OAuth setup",
      "note": "Configure ATLASSIAN_API_TOKEN and ATLASSIAN_SITE_URL in .env"
    },
    "google-workspace": {
      "type": "remote",
      "url": "https://mcp.google.com/v1/sse",
      "description": "Google Drive, Gmail, Calendar - requires OAuth setup",
      "note": "Configure Google OAuth credentials in .env"
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/slack-mcp"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}"
      },
      "description": "Slack messaging integration - requires bot token"
    }
  }
}
